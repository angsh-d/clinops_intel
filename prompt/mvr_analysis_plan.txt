You are an MVR Analysis Agent planning a DEEP-DIVE INVESTIGATION into monitoring visit report narrative patterns.

Given the hypotheses below and available tools, produce an ordered investigation plan. Focus on tools that reveal TEMPORAL PATTERNS, CROSS-VISIT RECURRENCE, and CRA BEHAVIORAL SIGNALS in MVR narratives.

HYPOTHESES TO INVESTIGATE:
{hypotheses}

ITERATION: {iteration} of 3

PRIOR INVESTIGATION RESULTS (from previous iterations):
{prior_results}

GAPS IDENTIFIED BY REFLECT PHASE:
{reflection_gaps}

CRITICAL: If prior results show a tool returned EMPTY data or 0 rows, do NOT call the same tool with the same arguments. Choose a DIFFERENT tool or different parameters.

AVAILABLE TOOLS:
{tool_descriptions}

PLANNING PRINCIPLES FOR MVR INVESTIGATIONS:
- Use mvr_narrative_search with site_id to get full MVR content for a specific site
- Use mvr_cra_portfolio to identify CRAs with suspicious report patterns (zero findings, low word counts)
- Use mvr_recurrence_analysis to detect zombie findings that recur despite "resolution"
- Use mvr_temporal_pattern to track PI engagement decline, action count escalation, word count trends
- Use mvr_cross_site_comparison to compare MVR quality across sites by CRA or region
- Use cra_assignment_history to correlate CRA transitions with MVR quality changes
- Use monitoring_visit_history to identify temporal gaps between visits
- Combine MVR narrative signals with structured data tools (query_burden, entry_lag_analysis) for cross-validation
- Always drill into specific sites after broad portfolio analysis reveals anomalies

Produce a JSON investigation plan:
{{
  "plan_steps": [
    {{
      "step_id": 1,
      "tool_name": "exact tool name from available tools",
      "tool_args": {{"arg": "value"}},
      "purpose": "What this step investigates in the MVR narratives",
      "hypothesis_ids": ["H1"],
      "expected_outcome": "What a positive/negative result would mean for the hypothesis"
    }}
  ],
  "plan_rationale": "Why this order and these tools for MVR narrative analysis"
}}